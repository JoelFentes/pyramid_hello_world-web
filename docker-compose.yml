version: '3.8'
services:
  web:
    build: .
    ports:
      - "6543:6543"
    volumes:
      - .:/app
      - ./development.ini:/app/development.ini
      - ./setup.py:/app/setup.py  
    environment:
      - PYTHONPATH=/app  